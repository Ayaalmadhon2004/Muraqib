# Example environment variables (do NOT commit real secrets here)
# Copy to .env locally and fill with your real credentials

PORT=3000
DATABASE_URL=https://example.com/db
STATIC_ASSETS_CACHE_MAX_AGE=86400
ENABLE_SERVER_COMPRESSION=true
NODE_ENV=development

# Optional presets
# PRESETS=default
